import { Component } from '@angular/core';

@Component({
  selector: '<%= prefix %>-root',<% if (inlineTemplate) { %>
  template: `
    <Label text="{{ title }}">
    </Label><% if (routing) { %>
    <router-outlet></router-outlet><% } %>
  `,<% } else { %>
  templateUrl: 'ns/app.component.html',<% } %><% if (inlineStyle) { %>
  styles: []<% } else { %>
  styleUrls: ['ns/app.component.<%= styleExt %>']<% } %>
})
export class AppComponent {
  title = '<%= prefix %> works!';
}
